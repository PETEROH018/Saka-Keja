import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import Navbar from '../Navbar/Navbar';
import Footer from '../Footer/Footer';
import { Pencil, ShieldCheck, Lightbulb } from 'lucide-react';

export default function EditProfile() {
  const navigate = useNavigate();

  const [formData, setFormData] = useState({
    fullName: 'Jane Smith',
    university: 'University of Nairobi',
    email: 'jane.smith@students.uonbi.ac.ke',
    phone: '+254 712 345 678',
    bio: 'Computer Science student looking for affordable housing near campus.'
  });

  const handleChange = (e) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log('Saved Profile:', formData);
    navigate('/StudentProfile');
  };

  return (
    <div className="min-h-screen flex flex-col bg-[#f8f8fb] text-[#1e1b4b] font-sans">
      <Navbar showSearch={false} />

      <main className="max-w-5xl w-full mx-auto px-6 pt-10 pb-16 flex-grow">
        <div className="mb-8">
          <h1 className="text-2xl font-bold text-[#0f172a] tracking-tight">Edit Profile</h1>
          <p className="text-gray-500 text-xs mt-1">
            Update your profile details to build trust with potential roommates and property managers.
          </p>
        </div>

        <form onSubmit={handleSubmit} className="grid grid-cols-1 lg:grid-cols-3 gap-6">
          {/* LEFT COLUMN - FORM FIELDS */}
          <div className="lg:col-span-2 space-y-6">
            {/* Personal Information */}
            <div className="bg-white rounded-xl p-6 border border-gray-100 shadow-xs">
              <h2 className="text-sm font-bold text-[#0f172a] pb-3 mb-4 border-b border-gray-100">
                Personal Information
              </h2>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs">
                <div>
                  <label className="block text-gray-600 font-medium mb-1">Full Name</label>
                  <input
                    type="text"
                    name="fullName"
                    value={formData.fullName}
                    onChange={handleChange}
                    className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-violet-600"
                  />
                </div>
                <div>
                  <label className="block text-gray-600 font-medium mb-1">University / Institution</label>
                  <input
                    type="text"
                    name="university"
                    value={formData.university}
                    onChange={handleChange}
                    className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-violet-600"
                  />
                </div>
              </div>
            </div>

            {/* Contact Details */}
            <div className="bg-white rounded-xl p-6 border border-gray-100 shadow-xs">
              <h2 className="text-sm font-bold text-[#0f172a] pb-3 mb-4 border-b border-gray-100">
                Contact Details
              </h2>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs">
                <div>
                  <label className="block text-gray-600 font-medium mb-1">Email Address</label>
                  <input
                    type="email"
                    name="email"
                    value={formData.email}
                    onChange={handleChange}
                    className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-violet-600"
                  />
                </div>
                <div>
                  <label className="block text-gray-600 font-medium mb-1">Phone Number</label>
                  <input
                    type="text"
                    name="phone"
                    value={formData.phone}
                    onChange={handleChange}
                    className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-violet-600"
                  />
                </div>
              </div>
            </div>

            {/* Professional Bio */}
            <div className="bg-white rounded-xl p-6 border border-gray-100 shadow-xs">
              <h2 className="text-sm font-bold text-[#0f172a] pb-3 mb-4 border-b border-gray-100">
                Student Bio
              </h2>
              <div className="text-xs">
                <label className="block text-gray-600 font-medium mb-1">Description</label>
                <textarea
                  rows="4"
                  name="bio"
                  value={formData.bio}
                  onChange={handleChange}
                  maxLength="500"
                  className="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:border-violet-600 resize-none text-xs"
                />
                <div className="text-right text-[10px] text-gray-400 mt-1">
                  {formData.bio.length}/500 characters
                </div>
              </div>
            </div>

            {/* Action Buttons */}
            <div className="flex justify-end space-x-3 pt-2">
              <button
                type="button"
                onClick={() => navigate('/StudentProfile')}
                className="px-4 py-2 border border-gray-300 rounded-lg text-xs font-medium text-gray-600 hover:bg-gray-50 transition"
              >
                Cancel
              </button>
              <button
                type="submit"
                className="px-5 py-2 bg-violet-700 hover:bg-violet-800 text-white rounded-lg text-xs font-medium transition shadow-xs"
              >
                Save Changes
              </button>
            </div>
          </div>

          {/* RIGHT COLUMN - SIDEBAR PANELS */}
          <div className="space-y-6">
            {/* Profile Picture Card */}
            <div className="bg-white rounded-xl p-6 border border-gray-100 shadow-xs flex flex-col items-center text-center">
              <div className="relative mb-3">
                <img
                  src="https://images.unsplash.com/photo-1494790108377-be9c29b29330?auto=format&fit=crop&q=80&w=150"
                  alt="Profile"
                  className="w-20 h-20 rounded-full object-cover border-2 border-violet-100"
                />
                <button
                  type="button"
                  aria-label="Edit Profile Picture"
                  className="absolute bottom-0 right-0 bg-violet-700 text-white p-1.5 rounded-full hover:bg-violet-800 transition"
                >
                  <Pencil className="w-3 h-3" />
                </button>
              </div>
              <h3 className="text-sm font-bold text-[#0f172a]">Profile Picture</h3>
              <p className="text-[11px] text-gray-500 mt-1 mb-3 leading-relaxed">
                A clear photo helps build trust with student housing managers.
              </p>
              <div className="flex items-center text-[11px] text-emerald-600 font-medium">
                <ShieldCheck className="w-3.5 h-3.5 mr-1" />
                Identity Verified
              </div>
            </div>

            {/* Pro Tip Card */}
            <div className="bg-violet-50/70 border border-violet-100 rounded-xl p-5 text-xs text-violet-900 flex items-start space-x-3">
              <Lightbulb className="w-5 h-5 text-violet-600 shrink-0 mt-0.5" />
              <div>
                <h4 className="font-semibold mb-1">Pro Tip</h4>
                <p className="text-[11px] text-violet-800 leading-relaxed">
                  Profiles with a complete bio and verified phone number receive 40% more responses from housing owners.
                </p>
              </div>
            </div>
          </div>
        </form>
      </main>

      <Footer />
    </div>
  );
}